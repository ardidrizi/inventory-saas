# Backend
NODE_ENV=development
PORT=4000
MONGO_URI=mongodb://localhost:27018/inventory?replicaSet=rs0
JWT_SECRET=replace_with_a_long_random_secret

# Optional: enables AI Insights endpoint on the backend.
# Keep this key server-side only and never expose it to frontend code.
OPENAI_API_KEY=
